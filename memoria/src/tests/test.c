#include "test.h"

/*
int test_dump() {
    //_______DEBUG____DUMP_____________

		create_mensaje_dump(1,1,3,4);
		create_mensaje_dump(2,1,4,5);
		create_mensaje_dump(3,1,2,7);
		create_mensaje_dump(4,0,NULL,NULL);
		dump();
		clear_mensajes_dump();

	//_____________________________________
    return 1;
}

int test_1() {
    //Chequeo si funcionan bien los calculos de cant de pag y marcos
    printf("Cantidad pag a alojar: %d \n", memory_getCantPag(datosConfigMemoria->tamanio, datosConfigMemoria->tamanio_pagina));
    printf("La direccion del marco 3 es: %d \n", memory_getAdressFrame(3, datosConfigMemoria->tamanio_pagina));
    //Simulo q se conecta un carpinchi y me pide 3 veces memoria
    memory_add_carpincho(carpinchos_list, 100);
    memory_add_carpincho(carpinchos_list, 10);
    uint32_t aa = memory_crearHeap(memoria_principal, 10);
    printf("Direccion 1: %d \n", aa);
    uint32_t ab = memory_crearHeap(memoria_principal, 5);
    printf("Direccion 2: %d \n", ab);
    uint32_t ac = memory_crearHeap(memoria_principal, 10);
    printf("Direccion 3: %d \n", ac);
}

int test_imprimir_cache() {
    escribir_en_cache(0,"PEPIPTO EL GRANDE", 30);
    cache_imprimir();    
}

*/
